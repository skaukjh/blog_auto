# 배치 파일은 두 가지 조건을 동시에 만족해야 cmd.exe가 실행할 수 있습니다.
#   1) 인코딩: CP949(ANSI)  - UTF-8이면 한글이 깨지고 명령까지 망가집니다
#   2) 줄바꿈: CRLF          - LF 단독이면 명령이 줄 중간에서 잘립니다
# git이 내용을 건드리지 않도록 바이너리로 취급해 바이트를 그대로 보존합니다.
*.bat -text
*.cmd -text
